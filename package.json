{
  "name": "receipt-portal",
  "version": "0.1.0",
  "private": true,
  "description": "A customer receipt portal built on Supabase — the fifth consumer of the ZEROFAYYZ FINTECH API.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed": "node --import tsx scripts/seed.ts",
    "test": "node --import tsx --test \"tests/**/*.integration-test.ts\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^7.2.0"
  }
}
